{
  "name": "eslint-hardcode-detect-plugin",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint -w eslint-plugin-hardcode-detect",
    "test": "npm test -w eslint-plugin-hardcode-detect"
  }
}
