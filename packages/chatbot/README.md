<!-- chatbot/README.md -->

# Chatbot (monólito)

Pacote **`chatbot/monolito`**: API NestJS, frontend Vite e testes E2E Playwright.

Este diretório é espelhado em **`tookyn/chatboot`** via `git subtree` (sem submodule). Ver `docs/chatbot-subtree.md` e `docs/chatbot-subtree-spec.md` no monorepo `tookyn/rating`.

Alvos no Makefile da raiz do monorepo: `make chatbot-subtree-help`, `make chatbot-subtree-push`, `make chatbot-subtree-pull`.
