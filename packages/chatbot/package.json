{
  "name": "chatbot-subtree-wrapper",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix api",
    "lint": "npm run lint --prefix api",
    "lint:fix": "npm run lint:fix --prefix api"
  }
}
