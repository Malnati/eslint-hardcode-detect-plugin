# sspa/docker-compose.frontend.yml
# MFEs e SSPA (Caddy e ngrok: caddy/docker-compose.yml e ngrok/docker-compose.yml). Agregado em docker-compose.yml (raiz).
services:
  sspa:
    profiles:
      - sspa
      - apps
      - full
    image: sspa-orchestrator:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sspa-orchestrator
    environment:
      SSPA_SKIP_AUTH: ${SSPA_SKIP_AUTH:-true}
    networks:
      - sspa-network
  mfe-account:
    profiles:
      - mfe
      - apps
      - full
    image: sspa-mfe-account:latest
  mfe-navigation:
    profiles:
      - mfe
      - apps
      - full
    image: sspa-mfe-navigation:latest
    build:
      context: .
      dockerfile: mfe-navigation/Dockerfile
    container_name: sspa-mfe-navigation
    restart: unless-stopped
    networks:
      - sspa-network
    healthcheck:
      test:
        - CMD
        - wget
        - -qO-
        - http://127.0.0.1/spa.js
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
  mfe-design-system:
    profiles:
      - mfe
      - apps
      - full
    image: sspa-mfe-design-system:latest
    build:
      context: .
      dockerfile: mfe-design-system/Dockerfile
    container_name: sspa-mfe-design-system
    restart: unless-stopped
    networks:
      - sspa-network
    healthcheck:
      test:
        - CMD
        - wget
        - -qO-
        - http://127.0.0.1/spa.js
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
  mfe-api:
    profiles:
      - mfe
      - apps
      - full
    image: sspa-mfe-api:latest
    build:
      context: .
      dockerfile: mfe-api-helpers/Dockerfile
    container_name: sspa-mfe-api
    restart: unless-stopped
    networks:
      - sspa-network
    healthcheck:
      test:
        - CMD
        - wget
        - -qO-
        - http://127.0.0.1/spa.js
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

networks:
  sspa-network:
    name: sspa-network
    driver: bridge
