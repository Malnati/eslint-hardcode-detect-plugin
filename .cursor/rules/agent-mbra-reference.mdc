---
description: Ponte normativa para agentes MBRA em reference/agents-ref (substituições e aplicabilidade)
alwaysApply: true
---

# Agentes de referência MBRA (`reference/agents-ref`)

Arquivos em [`reference/agents-ref/`](../../reference/agents-ref/) são **portfólio genérico** (MBRA). **Não** aplicar caminhos ou políticas de outros projetos (ex.: `docs/rup/`, `shared/constants.*`, `docs/review/`) sem adaptação.

**Fonte normativa:** [`specs/agent-mbra-reference-agents.md`](../../specs/agent-mbra-reference-agents.md) — tabela de aplicabilidade, substituições para este repo e hierarquia sobre os arquivos de referência.

Em dúvida, prevalecem [`AGENTS.md`](../../AGENTS.md) e os specs em [`specs/`](../../specs/).
